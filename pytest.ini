[pytest]
markers =
    integration: tests requiring external services (real Binance API, real Kafka)
    integration_pipeline: tests d'intégration pipeline Kafka-Spark-MongoDB (PySpark local, no external services)
addopts = -m "not integration"
