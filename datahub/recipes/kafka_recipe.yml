source:
  type: kafka
  config:
    connection:
      bootstrap: "kafka:9092"
    topic_patterns:
      allow:
        - "raw_trades"
        - "raw_klines"
        - "processed_data"
        - "anomalies"

sink:
  type: datahub-rest
  config:
    server: "http://datahub-gms:8080"
