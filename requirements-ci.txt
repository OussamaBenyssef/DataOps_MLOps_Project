# Lightweight requirements for CI (unit tests + DAG validation)
# Excludes Docker-only heavy deps: pyspark, tensorflow, apache-airflow-providers,
# acryl-datahub, confluent-kafka — those are installed in Docker images.

# Binance API
python-binance==1.0.19
websocket-client==1.7.0

# Kafka (lightweight client only)
kafka-python==2.0.2

# MongoDB
pymongo==4.6.1

# ML (lightweight, no TensorFlow/PySpark)
scikit-learn==1.4.0
pandas==2.1.4
numpy==1.26.3
mlflow==2.9.2
xgboost==2.0.3
ta==0.11.0
scipy==1.12.0
joblib==1.3.2

# Drift Detection
evidently==0.4.33

# API
fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.5.3
httpx==0.26.0

# Airflow (core only — providers are Docker-only)
apache-airflow==2.8.1

# Utils
python-dotenv==1.0.0
requests==2.32.3

# Testing
pytest==7.4.4
pytest-cov==4.1.0
